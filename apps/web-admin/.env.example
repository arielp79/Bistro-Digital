# Opcional: slug por defecto del admin
# VITE_TENANT_SLUG=bistro-digital

# Demo online (Netlify): URL pública de la API (sin barra final).
# En local dejar vacío — Vite proxea /api a localhost:3000.
# VITE_API_URL=https://tu-api.onrender.com

# URL del menú público (QR de mesas en /tables)
# VITE_WEB_CLIENT_URL=https://tu-menu.netlify.app

# Piloto restaurante: oculta Delivery IA, AFIP y setup Meta del menú lateral
# VITE_PILOT_CORE=true

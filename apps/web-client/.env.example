# ID de Mesa 1 del seed (npm run seed muestra el QR demo)
VITE_DEMO_TABLE_ID=

# Opcional en desarrollo: slug por defecto sin pantalla de selección (ej. bistro-digital)
# VITE_DEFAULT_TENANT_SLUG=bistro-digital

# Demo online (Netlify): URL pública de la API (sin barra final).
# En local dejar vacío — Vite proxea /api a localhost:3000.
# VITE_API_URL=https://tu-api.onrender.com
